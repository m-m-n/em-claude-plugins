{
  "name": "em-review",
  "description": "Parallel multi-perspective code review across 9 reviewers (5 Claude + 4 GPT/Codex). Cross-model agreement scoring + single-pass auto-fix on by default (batch preview → approve → atomic apply, modify-only, no commit; skip with --report-only). Each reviewer is also usable standalone via /em-review:<perspective>.",
  "author": {
    "name": "em"
  },
  "version": "0.1.10"
}
