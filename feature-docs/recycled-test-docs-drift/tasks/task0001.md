# Task Plan: task0001 — Correct the recycled-task-id-contract test records

## Goal

The two test records of feature recycled-task-id-contract (task0001.tests.yaml and task0002.tests.yaml) satisfy the tests.yaml schema in em-workflow/agents/implementer.md and describe the artifacts as they stand after the review round 1 auto-fix, so verify / retrospect can use them as attribution evidence without being misled.

## Requirements

FR1, FR2, FR3, FR4, FR5, FR6, NFR1, NFR2, NFR3, NFR4

## Scope

### Files to Create

None.

### Files to Modify
- `test-docs/recycled-task-id-contract/task0001.tests.yaml` — AC-3 red_reason quotation, AC-6 tests list and red_reason, AC-7 red_confirmed and red_reason, top-level notes (FR2 to FR6)
- `test-docs/recycled-task-id-contract/task0002.tests.yaml` — AC-4 entry gains red_confirmed false (FR1)

The FR4 measurement temporarily alters tests/test_hook_classification_pin.py in a working copy. That file is deliberately NOT in this task's file set: the measurement must leave it with no diff against the task's base and must leave no stray files under tests/ (NFR1). Nothing under em-workflow/ is touched, so no plugin version bump applies.

## Design

This task edits two YAML record files only. No code, no test, and no plugin file changes.

### Edit targets and required end state

| File | Entry / field | Required end state | Req |
|------|---------------|--------------------|-----|
| task0002.tests.yaml | acceptance_tests AC-4 | Has red_confirmed with value false. The existing empty tests list and the existing red_reason stay as they are. Key order inside the entry is tests, red_confirmed, red_reason (the same order as the other entries of that file). | FR1 |
| task0001.tests.yaml | acceptance_tests AC-3 red_reason | The passage that quoted "All four hooks detect a task as unlaunched solely from the absence ..." now quotes the current wording of em-workflow/references/implement-phase.md ("The other three queue hooks detect a task as **unlaunched** solely from the absence ..."). The quoted span is delimited by quotation marks so it can be extracted unambiguously, and it matches implement-phase.md character for character except for whitespace and line breaks (emphasis markers inside the span included). The string "All four hooks" no longer occurs anywhere in the file. | FR2 |
| task0001.tests.yaml | acceptance_tests AC-6 tests | Exactly seven identifiers: the four existing ones, unchanged, plus the three below in the file's existing module.Class.method notation. | FR3 |
| task0001.tests.yaml | acceptance_tests AC-6 red_reason | Keeps its existing explanation of the four pre-existing tests and adds the FR4 re-observation record (see the flow below). red_confirmed of AC-6 stays true in every outcome, including the unobserved fallback. | FR4 |
| task0001.tests.yaml | acceptance_tests AC-7 | red_confirmed is false. red_reason states both suite totals with the moment each was taken: 1493 (baseline 1452 plus 41 new, at task0001 implementation time) and 1506 (the integration branch after the review round 1 auto-fix was applied), worded so that 1506 is read as neither a task0001-only result nor the current total. red_reason also states why red_confirmed is false: an empty pre-implementation git diff is trivially true and is not an observed failure. The existing check items (2) to (4) remain. | FR5 |
| task0001.tests.yaml | top-level notes | No universal claim (such as "the red of every AC was re-confirmed") contradicts the AC-6 re-observation record or AC-7 red_confirmed false; such wording is narrowed to what was actually observed. | FR6 |

The three identifiers added to AC-6 (FR3):

- test_hook_classification_pin.TestObserveHookSource.test_bash_guard_reads_workflow_yaml_but_not_task_status
- test_hook_classification_pin.TestObserveHookSource.test_bare_workflow_yaml_mention_without_status_accessor_is_false
- test_hook_classification_pin.TestObserveHookSource.test_removing_the_status_carveout_flips_observation_to_false

### Preservation constraints (NFR3)

- task0001.tests.yaml: AC-1, AC-2, AC-4, AC-5 are unchanged. task0002.tests.yaml: AC-1, AC-2, AC-3, AC-5 are unchanged, and AC-4 changes only by the added red_confirmed line. task0003.tests.yaml is not touched.
- Fields not named in the table above (task_id, baseline_failures, final_failures, and so on) are unchanged.
- The edit is textual and local: diff hunks touch only the entries and fields named above. Re-serializing a whole file (which would reorder keys, re-wrap strings or drop comments in untouched entries) is not an acceptable way to make the change.
- Every edited scalar stays valid YAML: text containing colons, quotes or "#" is quoted or written as a block scalar, following the style the file already uses.

### FR4 re-observation flow

1. Resolve commit 48386b9 and its parent 48386b9^ (48386b9 is the review round 1 loop 1 auto-fix commit that rewrote reads_per_task_status; its parent carries the old rule, which judges only whether the docstring-stripped source contains "workflow.yaml").
2. Take the reads_per_task_status helper definition from tests/test_hook_classification_pin.py as it exists at 48386b9^.
3. In a working copy of the current tests/test_hook_classification_pin.py, replace only that helper's current definition with the 48386b9^ definition, keeping the three added tests exactly as they are now.
4. Run only the three FR3 tests against that working copy and record, for each test, the observed outcome: which assertion failed and what the failure said (or, if a test did not fail, that it passed).
5. Restore tests/test_hook_classification_pin.py to its base content; confirm it shows no diff against the task's base and that no stray file remains under tests/.
6. Write into AC-6 red_reason: (a) this is a re-observation performed in feature recycled-test-docs-drift, not an observation made at auto-fix time; (b) what was reverted (reads_per_task_status in tests/test_hook_classification_pin.py) and the base commit 48386b9^; (c) the observed outcome of each of the three tests, named individually.

Fallback: if the measurement cannot be carried out (48386b9 cannot be resolved, the old helper cannot be isolated, or the run fails for a reason unrelated to the assertions), AC-6 red_reason states plainly that the red of the three added tests is unobserved and gives the reason. Step 5 (no diff on tests/) applies in every case. Outcomes are recorded as observed — never as expected.

## Acceptance Criteria (MANDATORY)

- [ ] AC-1 (FR1): In task0002.tests.yaml, acceptance_tests AC-4 has red_confirmed with value false, alongside its unchanged empty tests list and unchanged red_reason. Every acceptance_tests entry in task0001.tests.yaml and task0002.tests.yaml has all three keys tests, red_confirmed and red_reason.
- [ ] AC-2 (FR2): The span quoted in task0001.tests.yaml AC-3 red_reason occurs in em-workflow/references/implement-phase.md once whitespace is normalized on both sides. The string "All four hooks" occurs 0 times in task0001.tests.yaml.
- [ ] AC-3 (FR3): task0001.tests.yaml AC-6 tests lists exactly seven identifiers: the four pre-existing ones and the three FR3 identifiers in module.Class.method notation. Each of the three names a test method that exists in tests/test_hook_classification_pin.py (exactly one match per method name).
- [ ] AC-4 (FR4): task0001.tests.yaml AC-6 red_reason states that it is a re-observation made in this feature, names the base commit 48386b9^ and what was reverted, and gives the observed outcome of each of the three tests individually — or, if the measurement could not be done, states explicitly that the red of the three tests is unobserved. tests/test_hook_classification_pin.py has no diff against the task's base.
- [ ] AC-5 (FR5): task0001.tests.yaml AC-7 red_confirmed is false. Its red_reason contains 1493 (1452 plus 41, at task0001 implementation time) and 1506 (the integration branch after the review round 1 auto-fix), each with when it was taken, plus the reason red_confirmed is false; the existing check items (2) to (4) are still present.
- [ ] AC-6 (FR6): task0001.tests.yaml notes contain no universal statement that contradicts the AC-6 re-observation record or AC-7 red_confirmed false.
- [ ] AC-7 (NFR1, NFR2, NFR3, NFR4): The diff against the task's base consists of exactly task0001.tests.yaml and task0002.tests.yaml under test-docs/recycled-task-id-contract/ (the task's own test record under test-docs/recycled-test-docs-drift/ excepted). The entries listed under Preservation constraints show no diff. Both files load as YAML. No test file is added. Running the project test command at the repository root finishes with 0 failures.

## Test Notes

NFR2 forbids new tests, so every criterion is verified by inspection plus the existing suite:

- AC-1, AC-3, AC-5: load both files as YAML and check the named keys, values and list contents.
- AC-2: string search for "All four hooks" in task0001.tests.yaml; extract the quoted span from AC-3 red_reason, normalize whitespace, and search for it in the whitespace-normalized implement-phase.md.
- AC-3: search each of the three method names in tests/test_hook_classification_pin.py.
- AC-4, AC-6: read the red_reason / notes text against the criterion; check tests/test_hook_classification_pin.py for a diff against base.
- AC-7: diff file list and hunks against base, YAML load of both files, and the project test command (python3 -m unittest discover -s tests).

In this task's own test record, each Acceptance Criterion gets an entry with an empty tests list and the inspection named in red_reason. Run each inspection once before editing: the pre-edit files violate AC-1 to AC-6, so those checks can be observed failing. A check that cannot fail before the edit (the scope and suite-pass parts of AC-7) is recorded with red_confirmed false and the reason, per implementer.md.

Edge cases:

- One of the three tests does not fail under the 48386b9^ helper: record that it passed; do not write an expected failure.
- The run under the old helper errors out before reaching the assertions: record the error as the observed outcome, or use the unobserved fallback if the error prevents a meaningful observation.

## Out of Scope

- Any change under em-workflow/ (implementer.md, implement-phase.md, review-phase.md included) and the plugin version bump.
- Deciding in review-phase.md who keeps tests.yaml in step after an auto-fix (to be filed as a separate task).
- task0003.tests.yaml, including its AC-6 / AC-7 / AC-8.
- Adding TestParseClassificationTableFailureModes or TestNonVacuityGuards to task0001 AC-6 tests (to be filed as a separate task).
- Changing the test-ID notation of any existing entry, or aligning task0001.tests.yaml with the Class::method notation of task0003.tests.yaml.
- Any change under tests/, and any new test.
