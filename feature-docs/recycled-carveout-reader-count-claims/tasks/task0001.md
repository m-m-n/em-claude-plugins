# Task Plan: task0001 — Narrow TS-13 row and its manual item to the AC-6 constants

## Goal

The TS-13 row (line 36) and the matching Manual Testing item (line 89) of `feature-docs/recycled-task-id-carveout/VERIFICATION.md` address only `HOOK_FILENAMES` and `JOURNAL_ONLY_HOOK_FILENAMES`, using the TS-13 criterion and the reader-site definition from IMPLEMENTATION.md Conventions. The docstring of `tests/test_recycled_task_id_consistency.py` is confirmed (not edited) to hold no reader-count claim.

## Requirements

FR1, FR2, FR3, FR4, NFR1, NFR2, NFR3

## Scope

### Files to Create

None.

### Files to Modify

- `feature-docs/recycled-task-id-carveout/VERIFICATION.md` — rewrite line 36 (TS-13 row) and line 89 (Manual Testing item). No other line changes.

## Design

### Target 1 — TS-13 row (line 36)

A row of the Test Scenarios table. After the rewrite:

- The ID cell is still `TS-13`, the Test Type cell is still `Inspection`, and the source note `(SPEC AC-6)` is still present.
- The Scenario cell names the module `tests/test_recycled_task_id_consistency.py` and the two constants `HOOK_FILENAMES` and `JOURNAL_ONLY_HOOK_FILENAMES`. No other constant identifier appears in the row.
- The Expected Result cell states the TS-13 criterion (retired with all references: no definition and 0 reader sites, or 2 or more reader sites) and the reader-site definition (code references outside the definition line; comments and docstrings not counted).
- The universal wording is removed: `No module-level constant ... is left with fewer than two reader sites`, `every constant has 0 (retired with its readers) or >= 2`, and any other wording that addresses every module-level constant.
- The row stays one physical line with the same number of cells as the table header row. It is written in English, like the rest of the file.
- State the criterion rather than measured counts or line numbers, so the row stays true as the module changes.

### Target 2 — Manual Testing item (line 89)

Current wording: "Count the reader sites of every module-level constant in that module and confirm none is left with exactly one (TS-13)."

After the rewrite:

- The item checks only `HOOK_FILENAMES` and `JOURNAL_ONLY_HOOK_FILENAMES`, applying the same criterion and reader-site definition as Target 1.
- The `(TS-13)` citation remains.
- The existing list-item / checkbox prefix is kept, and the item stays one physical line.

### Target 3 — test module docstring (confirmation only, no edit)

- Confirm that the module docstring of `tests/test_recycled_task_id_consistency.py` contains no reader-count claim: the phrase `leaving no module-level constant in this module with fewer than two reader sites` does not occur, and a case-insensitive search for `reader` finds no match in the docstring.
- The module is never edited (NFR2). If a reader-count claim is found at the implement base, report it as a plan deviation instead of editing.

### Reference facts (from SPEC, measured at d8e6d30)

- `HOOK_FILENAMES` has 3 reader sites (lines 1290, 1291, 1429 at measurement time).
- `JOURNAL_ONLY_HOOK_FILENAMES` has no definition and no reference.
- Other single-reader constants (`_PIN_PATH`, `PLUGIN_ROOT`, `IMPLEMENT_PHASE_PATH`, `STOP_CONDITION_3_PHRASE`, `ABORT_PHASE_TERMINAL_PHRASE`, `STATUS_NEVER_CONSULTED_PHRASE`, `ORCHESTRATOR_ONLY_SCOPE_PHRASE`) are outside TS-13's scope and are not mentioned.

## Acceptance Criteria (MANDATORY)

- [ ] AC-1: In line 36 of `feature-docs/recycled-task-id-carveout/VERIFICATION.md` (the TS-13 row), the only constant identifiers named are `HOOK_FILENAMES` and `JOURNAL_ONLY_HOOK_FILENAMES`. The Expected Result cell states the TS-13 criterion (no definition and 0 reader sites, or 2 or more reader sites) and the reader-site definition (code references outside the definition line; comments and docstrings not counted).
- [ ] AC-2: Line 36 contains neither `is left with fewer than two reader sites` nor `every constant has 0 (retired with its readers) or >= 2`, nor any other wording addressing every module-level constant. It still contains `TS-13`, `(SPEC AC-6)` and the Test Type `Inspection`, and it has the same number of table cells as the table header row.
- [ ] AC-3: Line 89 (the Manual Testing item) names only `HOOK_FILENAMES` and `JOURNAL_ONLY_HOOK_FILENAMES`, applies the same criterion and reader-site definition as AC-1, cites `TS-13`, and no longer contains `every module-level constant`.
- [ ] AC-4: Measured on the integrated `tests/test_recycled_task_id_consistency.py` using the reader-site definition, the TS-13 criterion holds: `HOOK_FILENAMES` has 2 or more reader sites, and `JOURNAL_ONLY_HOOK_FILENAMES` has neither a definition nor any reference.
- [ ] AC-5: The module docstring of `tests/test_recycled_task_id_consistency.py` contains no reader-count claim: a case-insensitive search for `reader` finds no match in it, and the phrase `leaving no module-level constant in this module with fewer than two reader sites` does not occur in the module.
- [ ] AC-6: Against the implement base commit, `feature-docs/recycled-task-id-carveout/VERIFICATION.md` differs only at lines 36 and 89, its line count is unchanged, and every other line (including TS-12, TS-14, the Verification Summary and line 105) is byte-identical. No other repository file changes, except workflow-generated records under `test-docs/recycled-carveout-reader-count-claims/`. In particular, nothing under `em-workflow/`, `.claude-plugin/marketplace.json`, `tests/` or the NFR2 file list changes.
- [ ] AC-7: `python3 -m unittest discover -s tests`, run from the repository root, exits with code 0.

## Test Notes

- Every criterion except AC-7 is an inspection check. FR4 allows no new file under `tests/`, so no test file is committed. Check AC-1 to AC-6 with read-only checks run inside the worktree: a line-level diff against the base, text searches for the removed phrases and for the two constant names, cell counting on the table row, and counting reader sites. Record them in the task's test record through the implement phase.
- AC-4 and AC-5 hold at the base already (per SPEC). Check them against the integrated module anyway, because the module may have changed since the SPEC measurement.
- Run the existing suite (AC-7) before and after the edit. A failure present before the edit is a pre-existing failure. A failure that appears only after the edit, caused by a test that pins the old wording of line 36 or 89, is a plan deviation to report. Do not edit `tests/`.

## Out of Scope

- The other single-reader constants listed under "Reference facts". They are not mentioned, counted or changed.
- Any line of `feature-docs/recycled-task-id-carveout/VERIFICATION.md` other than 36 and 89.
- The NFR2 files: `feature-docs/recycled-task-id-carveout/workflow.yaml`, `feature-docs/recycled-task-id-carveout/SPEC.md`, `feature-docs/recycled-task-id-carveout/tasks/task0004.md`, `test-docs/recycled-task-id-carveout/task0001.tests.yaml`, `test-docs/recycled-task-id-carveout/task0004.tests.yaml`, `tests/test_recycled_task_id_consistency.py`.
- Any file under `em-workflow/`, and any plugin version bump (NFR1).
- Adding new tests or test helpers to the repository.
