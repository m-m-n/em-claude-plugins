schema_version: 1
feature: destructive-guard-safe-delete-traversal
records:
  - question_id: codex.design-consultation.safe-delete
    packet_id: null
    answered_at: "2026-09-09T22:35:51+09:00"
    source: codex-consultation
    answer_mode: freeform
    selected_option_ids: []
    freeform: "normalize before the safe exception; component-wise containment; keep the class A / class B asymmetry; split DYNAMIC by escape capability so variable/command-substitution targets never take the safe exception while pure globs still do"
    normalized_answer: "FR1-FR7 の設計を確定。Codex の Q4 回答（SAFE_DELETE を DYNAMIC より先に維持）はケース表 110/112 行を ask から allow へ退行させるため却下し、requirements-analyst の 2 分割案を採用した。"
    resolution_note: "gate: none (user-instructed design consultation, not a gate); codex_consulted: true; wrapper: em-workflow/scripts/run_codex_exec.sh readonly; provider: fallback entry 2 (litellm / vertex-glm-5.2) after entry 1 timed out at 600s; full record: feature-docs/destructive-guard-safe-delete-traversal/codex-consultation.md"
  - question_id: implement.task-branch-cleanup-denied
    packet_id: null
    answered_at: "2026-09-12T22:17:00+09:00"
    source: unlisted-gate-fallback
    answer_mode: freeform
    selected_option_ids: []
    freeform: "use git branch -d instead of git branch -D"
    normalized_answer: "task0003 のブランチ掃除で destructive-guard が `git branch -D` を拒否した（理由: 未マージブランチの強制削除）。マージは merge-base --is-ancestor で検証済みのため、副作用がより小さく未マージなら自動で拒否する `git branch -d` を integration worktree（HEAD = integration ブランチ）から実行して掃除を完了した。"
    resolution_note: "gate: none (PreToolUse hook denial, no gate_id); codex_consulted: false; rationale: minimum-side-effect option per references/batch-mode.md unlisted-gate fallback; the non-forcing form refuses when unmerged, so it is strictly safer; implement-phase.md's note that the non-forcing form would REFUSE here assumes the main working tree, not the integration worktree"
  - question_id: review.auto-fix-judgment
    packet_id: null
    answered_at: "2026-09-12T22:52:00+09:00"
    source: user-instructed-consultation
    answer_mode: single
    selected_option_ids: [skip-both]
    freeform: null
    normalized_answer: "round 2 の high 2 件（c4e8db6735e86e22 / 8330e432c5c1600b）は prose のため needs-judgment に分類され、batch 既定では Apply as-is になる。ユーザー指示に従って相談し、(a) 両方に editor を出す / (b) 片方だけ出す / (c) 両方 skip して deferred にする の 3 択から (c) を採用した。auto-fix は実行していない。"
    resolution_note: "gate: review.auto-fix-judgment (batch default is Apply as-is); overridden by the invocation's explicit instruction to consult Codex and adopt the comparatively better option; codex_consulted: attempted, NOT reached (codex exec returned a usage-limit error, reset ~2026-09-13 01:02); fallback provider answered after reading destructive-guard.py and destructive-guard-cases.json; opus_escalation: not run; option chosen: (c) skip both, record as deferred; options not chosen: (a) dispatch editors for both, (b) dispatch an editor for one; key points: the prescription is prose requiring a broad rewrite of the decision core (check_git alone has ~15 decide() call sites), no non-rm check currently emits allow so a faulty pooling refactor could turn a correct deny into a real allow, the case table has zero rm-plus-non-rm cases so 178/178 can stay green after a bad application (verified), round 1 on this same file with the same batch Apply-as-is path broke 4 tests and 2 SPEC-pinned rows and was rolled back, and F2/F3's worst case under batch is a wrong rule name on an already-correct deny; also judged F2 and F3 to be the same defect split only by the 5-line same_site window"
