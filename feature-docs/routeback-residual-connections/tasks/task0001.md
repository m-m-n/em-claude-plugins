# Task Plan: task0001 — Close I.2.a/I.2.c route-back residual connections, pin them with contract tests, bump em-workflow to 0.2.1

## Goal

Every claim on Step I.2.a / Step I.2.c's route-back path in
`em-workflow/references/implement-phase.md` follows from what the document actually
verifies. Specifically:

- the carve-out / gate / reconciled-state chain has a stated end point;
- the reset target set covers both `failed` sources;
- the cleanup's not-merged claim is limited to what is read, and the undetected residual
  is recorded.

Each connection is pinned by a document-contract test with a negative proof, and
em-workflow ships as 0.2.1.

## Requirements

FR1, FR2, FR3, FR4, FR5, FR6, FR7, NFR1, NFR2, NFR3, NFR4, NFR5, NFR6, NFR7, NFR8, NFR9

## Scope

### Files to Create

- `tests/test_routeback_residual_connections_version_bump.py`: TS-5, the em-workflow
  version lockstep and baseline test (IMPLEMENTATION.md D5).
- `test-docs/routeback-residual-connections/task0001.tests.yaml`: the per-task test
  record the implement phase requires.

### Files to Modify

- `em-workflow/references/implement-phase.md`: Step I.2.a gains the chain-termination
  sentence (Site B). Step I.2.c gets the reset target union and its connecting sentence
  (Site C), the cleanup claim rewrite (Site D), and the residual sentence (Site E).
- `tests/test_routeback_reset_scope_consistency.py`: additive document-contract tests
  for FR1–FR5 (Site F), plus an appended docstring section.
- `em-workflow/.claude-plugin/plugin.json`: version 0.2.0 → 0.2.1.
- `.claude-plugin/marketplace.json`: the em-workflow entry's version 0.2.0 → 0.2.1. The
  em-review entry does not change.

Edit-forbidden, and must stay byte-identical: `tests/test_recycled_task_id_consistency.py`
and `tests/test_implement_routeback_gate.py`. You may read them to learn their anchors.
You may also read `tests/test_routeback_reset_scope_version_bump.py` as the shape
reference for the new version test.

## Design

Follow IMPLEMENTATION.md Conventions C1–C8 throughout. The sites below are identified by
text. Tokens called "first-occurrence-sensitive" are located by existing tests at their
first occurrence in normalized I.2.c. New I.2.c prose must not contain any of them:

- `tasks.{T}.status` (inside or before the write set)
- `ROUTEBACK_TIP`
- `reset --hard`
- `commit-docs.sh`
- `git worktree remove --force`
- `git branch -D`
- "make one ordered workflow.yaml write set"
- "Commit that write set next, BEFORE any cleanup"
- "Only once that commit"
- "End the phase with a"
- "When the gate does not hold"
- `TERMINAL_TIP`
- "- **abort phase**"
- "`create-plan` to `needs_update`"
- "terminal journal last event (`merged` or `failed`)"
- "this order's one residual leftover state"

### Site A: I.2.a forward reference (FR1). No document edit.

The premise "Because Step I.2.c's route-back gate below blocks route-back whenever any
task's journal last event is `merged` — read from the journal directly, independent of
the ancestor check —" already holds and is not edited.

Its referent is I.2.c's third conjunct: the one that makes route-back inadmissible
whenever the last-event-per-task replay alone reports a journal last event `merged`,
regardless of the ancestor verification.

Site A exists only to be pinned by tests (Site F, TS-1).

### Site B: I.2.a chain-termination sentence (FR2)

Insert exactly one sentence after the sentence that ends "…the recycled-task-id
carve-out above stays correctly scoped to `failed` only.", and before "The
recycled-task-id carve-out above is applied by two parties".

Required content:

1. The carve-out reclassifies only a task whose journal last event is `failed` and whose
   workflow.yaml `status` is `pending`.
2. It never touches a `merged` or a `launched` journal last event.
3. So Step I.2.b step 1's `merged` and in-flight classifications never consult the
   carve-out. These classifications are the inputs Step I.2.c's gate reads, and both
   steps are referenced as "below".
4. So the chain I.2.a carve-out ← I.2.c gate ← I.2.b step 1 reconciled state ← I.2.a
   carve-out terminates there and is not circular.

Frame items 2–3 as "the carve-out's outcome cannot change those classifications". That
keeps the sentence consistent with I.2.c's `in_progress` half, which still says "(a
`launched` last event, with the recycled-task-id carve-out that step already defines)".
Do not edit that wording.

Preserved I.2.a anchors (NFR6):

- The span from "Because Step I.2.c's route-back gate below blocks route-back whenever
  any task's journal last event is `merged`" to the first "correctly scoped to `failed`
  only." keeps exactly one "Because " and exactly one " so ". This is why the new
  sentence goes after that span.
- The raw `Select` line-wrap literal is not reflowed.
- These survive unchanged: the unreachability sentence ("Given I.2.c's route-back
  precondition" … "can never arise."), the recursion-invariant sentence, "This carve-out
  is deliberately scoped to `failed` only", the in-flight sentence, the two-parties
  sentence, and the hook-group sentences.
- I.2.a gains none of the following:
  - "task0001"
  - "renumber"
  - "governs only"
  - "the third route-back gate conjunct above"
  - "Because route-back proceeds only when no task is `merged` under either source"
  - "no retired task id can leave a `merged` last event behind for a renumbered task to
    inherit"
  - "never read workflow.yaml" / "never reads workflow.yaml"
  - a second "can never arise."
  - "Given I.2.c's route-back precondition"
  - "The other three queue hooks —"
  - "and the Stop hook,"

### Site C: I.2.c reset target set and its connection (FR3)

In the route-back write set, the reset target set becomes the union of two members:

1. "every task whose Step I.2.b step 1 reconciled state is `failed`". This literal stays
   verbatim and leads.
2. Every task that workflow.yaml reports as `status: failed`. Phrase it without the
   `tasks.{T}.status` token.

Add one sentence before "Commit that write set next, BEFORE any cleanup". It states:

- The two members can diverge. Cite Step I.2.b step 3 as the owning rule for
  workflow.yaml's `failed` write, and do not restate that rule.
- The postcondition "no task is left `merged` or `in_progress` or `failed`" and
  `references/workflow-patch.md`'s `replace_all` permission conditions / protocol-error
  rule are read off workflow.yaml's own statuses.
- Covering both members is what makes that postcondition true.

The gate runs before the write set, so the union admits nothing the gate blocks (EC-4).
The new text must not suggest otherwise.

Preserved I.2.c anchors (NFR5):

- The first `tasks.{T}.status` in normalized I.2.c keeps `pending` within the next 60
  characters.
- The four write tokens precede "git worktree remove --force": "`create-plan` to
  `needs_update`", "`implement` step back to `pending`", "`tasks.{T}.status` back to
  `pending`", and "`tasks.{T}.notes`".
- This order holds: gate < `ROUTEBACK_TIP` < `reset --hard` < "make one ordered
  workflow.yaml write set" < "Commit that write set next, BEFORE any cleanup" < "Only
  once that commit".
- These survive: "replace_planning", "references/workflow-patch.md", "replace_all",
  "failure reason", and the retained gate literals.

### Site D: I.2.c cleanup sentence (FR4, first part of FR5)

- Remove the unqualified "confirmed not merged".
- In its place, state a not-merged claim limited to the two sources this path reads:
  neither workflow.yaml `status` nor Step I.2.b step 1's reconciled state reports any of
  these tasks `merged`.
- Give the gate above as the reason no reconciled-`merged` task is ever a cleanup
  target. The gate already refused route-back whenever such a task exists, so the set the
  write set just reset contains none. The gate stays the single owner of that decision.
  The cleanup sentence states a consequence and adds no filter of its own.
- These retained literals survive verbatim:
  - "clean up worktrees and branches for exactly the tasks the write set just reset"
  - "a task whose reconciled state is `merged` is never a cleanup target, whatever
    workflow.yaml says"
  - the parenthesized worktree/branch removal, ending "for every {T} just reset"
  - "this order's one residual leftover state is the commit succeeding and the cleanup
    not yet running"
  - "Step I.2.a's resume guard and its recycled-task-id rule already cover"
- `git branch -D` occurs exactly once in I.2.c, between the cleanup-scope phrase and the
  leftover-state sentence.

### Site E: I.2.c residual sentence (second part of FR5)

Insert one sentence after the leftover-state sentence (ending "…already cover.") and
before "End the phase with a clear report".

Required content:

1. What neither source can see: a task whose merge already advanced the integration
   branch through merge-task.sh's `git update-ref`, while its `merged` event never
   reached the journal.
2. When that happens: merge-task.sh's journal write fails (the script reports a warning
   and still exits 0), or the implementer stops between the ref update and the journal
   write and leaves no report.
3. Such a task is indistinguishable here from a failed task, so route-back can still
   reset it and delete its branch.
4. This is a known residual that the gate does not close. The sentence adds no
   verification step and changes no behaviour.

Constraints (C3, C5):

- The sentence contains no "append", "appended", "rework", and not the merge-task.sh
  function name.
- It contains no literal `git branch -D`.
- `git update-ref` stays in mid-line.
- It cites merge-task.sh as the owner of the ref-update / journal-write behaviour.

### Site F: document-contract tests (FR6) in `tests/test_routeback_reset_scope_consistency.py`

All edits are additive (C7), follow C6, and use the module's existing section helpers and
normalization.

- **TS-1 (FR1).**
  - A direction constant naming Step I.2.c's route-back gate as `below` is present in
    normalized I.2.a.
  - A referent constant, a phrase of I.2.c's third conjunct stating the journal-`merged`
    block, is present in normalized I.2.c.
  - An absence matcher finds no I.2.c gate or conjunct reference followed by "above" in
    normalized I.2.a.
  - Keep the absence matcher tight: it must match an I.2.c gate/conjunct reference and
    its direction word within the same phrase. I.2.a legitimately says "the
    recycled-task-id carve-out above", including in the new Site B sentence, and a loose
    "I.2.c … above" window would catch that.
  - Negative proof: the module's existing pre-change sample carrying "(the widened I.2.c
    gate above)" (D3) contains the `above` form and lacks the direction constant. That
    sample's existing non-vacuity guard is reused.
- **TS-2 (FR2).**
  - Constants pin the termination sentence's distinctive clauses: the `failed` +
    `pending`-only reclassification, `merged` / `launched` untouched, and the chain
    terminating.
  - A placement test puts the sentence after the first "correctly scoped to `failed`
    only." and before "is applied by two parties".
  - Negative proof: a verbatim 9f95024 excerpt of I.2.a, from the premise through the
    start of the two-parties sentence.
- **TS-3 (FR3).**
  - A constant for the second member is present, positioned after the reconciled-state
    literal (leading).
  - A slice of the connecting sentence contains the Step I.2.b step 3 citation and the
    `replace_all` reference. Slice to the sentence: "replace_all" already occurs
    elsewhere in I.2.c, so a whole-section presence check proves nothing.
  - Negative proof: a verbatim 9f95024 excerpt of the write set, from "then make one
    ordered workflow.yaml write set" through the `replace_all` citation.
- **TS-4 (FR4, FR5).**
  - The gate-reason constant sits inside the cleanup sentence's slice, which runs from
    "Only once that commit" to the leftover-state sentence.
  - The source-qualified constant is present.
  - The bare "confirmed not merged" is absent from normalized I.2.c.
  - The residual constant sits after the leftover-state sentence and before "End the
    phase with a", and its slice names merge-task.sh.
  - Negative proof: a verbatim 9f95024 excerpt of the cleanup, from "Only once that
    commit" through "already cover.". It contains "confirmed not merged" and lacks every
    new constant.
- **Non-vacuity guards.** Give each new excerpt a guard on a retained anchor that is
  present in both the excerpt and the live document, such as "correctly scoped to
  `failed` only", "`create-plan` to `needs_update`", or "clean up worktrees and branches
  for exactly the tasks the write set just reset".
- **Docstring.** Append a section for this feature. It maps TS-1..TS-4 to the classes,
  lists every new matcher with its proof, and records both capture revisions: `9f95024…`
  for FR2–FR5 and `b3d8824…` for FR1's reused sample.

### Site G: version (FR7)

- Set em-workflow to 0.2.1 in both manifests.
- The new version module parses both manifests with the standard library and finds the
  em-workflow entry in the marketplace. It asserts that the version is strictly greater
  than (0, 2, 0) by per-component numeric comparison and that both manifests are equal.
- Negative proofs: a forged "0.2.0" is not accepted as greater, and a forged differing
  pair is detected as unequal. Run both through the same comparison the positive tests
  use.
- Non-vacuity: the em-workflow marketplace entry is found.

### Commit shape (C8)

Stage the `implement-phase.md` edit and both manifest bumps in one commit. Test files may
be committed separately, before or after that commit.

## Acceptance Criteria (MANDATORY)

- [ ] AC-1 (FR1; TS-1):
  - Normalized I.2.a names Step I.2.c's route-back gate as `below` in the
    journal-`merged` premise, and that premise sentence is unchanged.
  - Normalized I.2.c contains the third-conjunct phrase that blocks route-back on a
    journal last event `merged`.
  - No I.2.c gate or conjunct reference in normalized I.2.a is followed by "above".
  - The direction and `above` matchers are proven against the existing "(the widened
    I.2.c gate above)" sample.
- [ ] AC-2 (FR2; TS-2):
  - I.2.a contains one new sentence, after the first "correctly scoped to `failed`
    only." and before "is applied by two parties".
  - The sentence states: (a) the carve-out reclassifies only a journal last event
    `failed` paired with workflow.yaml `pending`; (b) it never touches a `merged` or
    `launched` last event; (c) Step I.2.b step 1's `merged` and in-flight
    classifications therefore do not consult it; (d) so the carve-out / gate /
    reconciled-state chain terminates.
  - The premise-to-first-"correctly scoped to `failed` only." span still has exactly one
    "Because " and one " so ".
  - Negative proof against the 9f95024 I.2.a excerpt.
- [ ] AC-3 (FR3; TS-3):
  - I.2.c's reset target set names "every task whose Step I.2.b step 1 reconciled state
    is `failed`" verbatim and first, then every task workflow.yaml reports
    `status: failed`.
  - One sentence placed before "Commit that write set next, BEFORE any cleanup" cites
    Step I.2.b step 3 for the divergence without restating it. It states that the
    postcondition and `references/workflow-patch.md`'s `replace_all` permission
    conditions are read off workflow.yaml's own statuses, so covering both members makes
    the postcondition true.
  - The first `tasks.{T}.status` in normalized I.2.c still has `pending` within 60
    characters.
  - Negative proof against the 9f95024 write-set excerpt.
- [ ] AC-4 (FR4, FR5; TS-4):
  - The cleanup sentence gives the gate above as the reason no reconciled-`merged` task
    is a cleanup target, and both retained cleanup literals survive verbatim.
  - The bare "confirmed not merged" is absent from normalized I.2.c, and the not-merged
    claim names only workflow.yaml `status` and Step I.2.b step 1's reconciled state.
  - A residual sentence sits between the leftover-state sentence and "End the phase with
    a". It names merge-task.sh's `git update-ref`, the journal-write-failure window
    (warning, exit 0) and the stop-without-report window. It states that route-back can
    reset such a task and delete its branch, as a known residual the gate does not close.
  - `git branch -D` occurs exactly once in I.2.c.
  - Negative proof against the 9f95024 cleanup excerpt.
- [ ] AC-5 (FR6, NFR9):
  - Every new-wording literal is one module-level constant, read by both its positive
    assertion and its negative proof.
  - Every new excerpt is copied verbatim, with raw line breaks, from the named revision
    and has a non-vacuity guard on a retained anchor present in both the excerpt and the
    live document.
  - The module docstring gains a section with the full matcher-to-proof inventory and
    the capture revisions.
  - Only the standard library is used, and pre-existing constants, methods and classes
    are byte-identical.
- [ ] AC-6 (FR7; TS-5):
  - `em-workflow/.claude-plugin/plugin.json` and the em-workflow entry of
    `.claude-plugin/marketplace.json` both read 0.2.1. The em-review entry is unchanged.
  - The new version module asserts strictly-greater-than-0.2.0 (per-component numeric)
    and equality.
  - Negative proofs against a forged 0.2.0 and a forged differing pair.
- [ ] AC-7 (NFR1–NFR7; TS-6, TS-7):
  - `python3 -m unittest discover -s tests` exits 0 from the repository root.
  - The two edit-forbidden modules are byte-identical to the base.
  - Changed files are limited to this task's Scope.
  - Normalized I.2.c contains neither "append" nor "rework".
  - No line of `implement-phase.md`, after stripping indentation and backticks, starts
    with `git ` and contains `commit` or `add -A`.
  - The I.2.c heading and the batch-mode tail paragraph stay byte-identical.

## Test Notes

- **TDD order.**
  - Write the Site F tests first. The FR2–FR5 positive tests go red against the current
    document, and the version test goes red on 0.2.0. Record these reds in
    `task0001.tests.yaml`.
  - FR1's positive tests are green on the base by design (it is a pin). Its red is shown
    by the negative proof against the older sample. Record that proof as the observed
    red for AC-1.
- **Capture.** Read `implement-phase.md` as it exists at revision
  `9f9502487a8da29220aece87f253058becda432e` from the git object store, and copy each
  excerpt byte-for-byte, keeping the original line breaks and indentation. Before
  relying on an excerpt, confirm it contains its guard anchor. If the revision cannot be
  read, stop and report a deviation (IMPLEMENTATION.md D4).
- **Pin clauses, not whole sentences.** Constants should hold distinctive clauses, so
  the positive/negative pairing stays meaningful without freezing every word of a new
  sentence.
- **Sentence-scoped checks** (FR3's citation, FR4's gate reason, FR5's merge-task.sh
  mention) slice between anchors that exist exactly once. A whole-section presence check
  is vacuous for tokens that already occur elsewhere in I.2.c.
- **Before every commit**, run the full suite, not only this module. Many anchors this
  task can break live in the two edit-forbidden modules.
- **Out-of-scan modules.** A version module outside the scanned set (for example
  `tests/test_recycled_task_id_version_bump.py`) might assume a shape different from
  SPEC A-8. If one then fails on 0.2.1, that is a deviation to report. Do not edit that
  module to make it pass.

## Out of Scope

- Any change to `em-workflow/scripts/merge-task.sh`, hooks, agents, skills, or journal
  writers (NFR1).
- Adding an ancestor check before cleanup (IMPLEMENTATION.md D2).
- Re-editing the FR1 premise sentence, the Branch & Worktree Model exit-4 bullet, the
  I.2.c gate text, the rejected-path text, the abort-phase bullet, or the batch-mode
  paragraph.
- EC-3: specifying cleanup behaviour for a reset task with no worktree or branch.
- The watchdog-kill `launched` failure-net gap, and I.2.b step 3's own precedence
  ambiguity.
- Rewording pre-existing docstring prose in `tests/test_routeback_reset_scope_consistency.py`,
  for example its earlier "confirmed not merged" description. Append only.
