# Task Plan: task0008 — agent boundary wording, symlink-safe append, README consistency

## Goal

The remaining medium-severity consistency and hardening findings are
resolved: designer's read boundary matches its input contract, the
.gitignore append is symlink-safe, test/README.md presence probing uses
tracked-ness, and the README documents the single .gitignore exception.

## Requirements

FR1, FR6, FR7, NFR1

## Scope

### Files to Create
- (none)

### Files to Modify
- `em-workflow/agents/designer.md` — WRITE-only boundary wording
- `em-workflow/agents/requirements-spec-creator.md` — symlink-safe .gitignore append + tracked-ness probe for test/README.md
- `em-workflow/README.md` — clean-main invariant wording (single .gitignore exception)

## Design

Rework of review round 1 residual findings (reviews/round1.yaml):
4be72ffc99bb4e33, d86e39e805002f56, 56d0a31fd4ae5a58, 8f4e0dc87917e6a6.
Their suggestions are authoritative; key decisions:

1. **designer.md D0**: boundary restated as WRITE-only — nothing writes to
   the main working tree; reading user-provided inputs under project-root
   `tmp/` is an explicit allowed exception (matching
   requirements-spec-creator's Phase 0.5 wording).
2. **requirements-spec-creator.md Phase 3 ignore gate**: before the fallback
   append, require the root `.gitignore` to be absent or a regular
   non-symlink file; a symlink or non-regular file aborts with a report
   instead of writing through it.
3. **requirements-spec-creator.md Phase 0.5**: the test/README.md presence
   check uses tracked-ness (git's tracked-file listing) rather than
   working-tree existence, OR Phase 3 re-checks inside the worktree and
   creates from the template when absent — untracked-in-main copies must not
   suppress creation in the worktree.
4. **README.md**: the clean-main statement names the single tolerated
   exception (the gitignore-guard `.claude/worktrees/` append) consistently
   with the execution protocol.

## Acceptance Criteria (MANDATORY)

- [ ] AC-1: designer.md contains no "reads from ... the main working tree"
  prohibition that contradicts its tmp/ input instructions; the boundary is
  WRITE-only with the tmp/ read exception explicit.
- [ ] AC-2: The Phase 3 append instruction rejects symlink/non-regular
  .gitignore before writing.
- [ ] AC-3: Phase 0.5/Phase 3 semantics guarantee test/README.md exists in
  the worktree even when an untracked copy exists only in the main tree.
- [ ] AC-4: README.md's invariant sentence documents the .gitignore
  exception; no absolute "一切変更されない" claim remains without it.

## Test Notes

- Documentation task: verification by inspection/grep. The structural test
  suite (tests/test_planner_designer_worktree_docs.py) must remain green.

## Out of Scope

- commit-docs.sh / tests (task0006), protocol lock contracts (task0007).
- Performance findings 94344ce04158d99f / 44036345af317ef7 (cross-model
  disputed; deliberately not reworked this round).
