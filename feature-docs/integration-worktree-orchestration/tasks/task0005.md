# Task Plan: task0005 — planner/designer path updates, README, version bump

## Goal

The remaining agent instructions write their outputs into the integration
worktree, and the plugin's public documentation and version reflect the new
model.

## Requirements

FR6, FR7

## Scope

### Files to Create
- (none)

### Files to Modify
- `em-workflow/agents/implementation-planner.md` — output paths (IMPLEMENTATION.md / VERIFICATION.md / tasks/ / workflow.yaml) + commit instructions
- `em-workflow/agents/designer.md` — output paths (DESIGN.md, design-system/, mockups) + commit instructions
- `em-workflow/README.md` — branch/worktree model description (clean-main-tree guarantee, branch-based resume)
- `em-workflow/.claude-plugin/plugin.json` — description update + patch version bump

## Design

Follows IMPLEMENTATION.md D1/D2/D3 and the Doc-commit convention.

Document changes:
1. implementation-planner.md: all reads (workflow.yaml, SPEC.md,
   REQUIREMENTS.md, LESSONS.md 記載はそのまま) and writes resolve inside the
   integration worktree; writes end with a commit-docs.sh call.
2. designer.md: DESIGN.md / design-system/ / mockup outputs are
   worktree-resident with commits; no main-tree writes.
3. README.md: the develop flow description states artifacts live committed on
   the integration branch, main stays clean until the final merge, and
   resume is branch-based (new format only).
4. plugin.json: description sentence(s) covering the branch/worktree model
   updated; version bumped by one patch.

## Acceptance Criteria (MANDATORY)

- [ ] AC-1: Both agent documents write all outputs to worktree paths followed
  by commit-docs.sh commit instructions; no main-tree artifact writes remain.
- [ ] AC-2: README.md describes the worktree-resident committed model and
  branch-based resume; no untracked-in-main wording remains.
- [ ] AC-3: plugin.json version is bumped exactly one patch and its
  description reflects the new model.

## Test Notes

- Documentation task: verification by inspection/grep (M-1 audit) and the
  existing tests/test_hooks_registration.py-style JSON validity check if one
  applies; plugin.json must remain valid JSON (parseable).

## Out of Scope

- git-setup-guard / gitignore-guard agents (behavior unchanged).
- marketplace.json (no version field per plugin there).
- LESSONS.md mechanics and retrospect flow documents.
