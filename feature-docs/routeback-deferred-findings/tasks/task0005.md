# Task Plan: task0005 — workflow-schema.md records `merge-unverified` and the full `agents.jsonl` reader set

## Goal

`em-workflow/references/workflow-schema.md` records `merge-unverified` as a third
additive value of the journal's `failed` reason field without adding a writer, states
the complete `agents.jsonl` reader set and what an absent or stale index costs
(finding `94de80a315821a55`), and keeps its Status semantics unconditional — pinned by
a new doc-contract module (findings `59e58726b1b22211` / `9cc0ce6b64087d2f` on the SSOT
side).

## Requirements

FR6, FR7, FR8, FR10, NFR1, NFR4, NFR8

## Scope

### Files to Create

- `tests/test_workflow_schema_journal_readers_doc_contract.py` — doc-contract tests for TS-6 and the workflow-schema half of TS-10

### Files to Modify

- `em-workflow/references/workflow-schema.md` — the journal writer-set paragraph and the agents.jsonl paragraph only

## Design

Normative references: IMPLEMENTATION.md SC-1, SC-3, SC-4, SC-5, D3, D5, D6,
Conventions 1, 2, 4, 6. Every other line of the file, including the Status semantics
section and the `failed_kind` definition, stays byte-identical.

### R1 — journal writer-set paragraph

1. The sentence that makes the helper "invoked only by the orchestrator's I.2.b
   orphan-recovery attempt" is adjusted so it stays true: the helper's invocation
   sites are the I.2.b orphan-recovery attempt (existing citation, "I.2.b Recovery /
   Residual block") and I.2.b step 1's ancestor-check branch (SC-4 label).
2. A new statement records that the same helper, invoked only from
   "`em-workflow/references/implement-phase.md`'s I.2.b step 1 ancestor-check branch"
   (cited, not restated), also writes `failed` with reason `merge-unverified`, "a third
   additive value of the existing `failed` reason field", adding no writer.
3. Constraints: between "Its writer set is unambiguous" and the first following
   "outside this one exception", the backtick-quoted script names ending in .py or .sh
   remain exactly the five writers (`merge-task.sh`, `queue_launch_guard.py`,
   `queue_failure_net.py`, `queue_taskstop_net.py`, `journal-append-failed.py`) — so no
   other script name (for example `recover-orphaned-task.py`) is backtick-quoted inside
   that span; "outside this one exception" stays; no SC6 code appears.
4. Existing phrases stay: "an additive value of the existing `failed` reason field"
   (the `orphaned` sentence), "also appends `failed` with reason `stale-launched`, a
   second additive value of the existing `failed` reason field, adding no writer", "no
   existing event name or reason is renamed or removed", and the citation
   "`em-workflow/references/implement-phase.md`'s I.2.b Recovery / Residual block".

### R2 — agents.jsonl paragraph

1. Replace the reader statement so all four readers are named: `queue_taskstop_net.py`
   at stop; `queue_failure_net.py`'s agent-index fallback; the orchestrator's I.2.b
   step 1 Agent index lookup and Recovery (the "Orchestrator-side read" in
   `em-workflow/references/implement-phase.md`'s Supporting cast, cited — its rule is
   not restated, D6); `recover-orphaned-task.py`.
2. Replace the absence statement: an absent or stale index degrades the stop-tool
   recorder to a no-op, leaves the failure net's fallback unresolved, and also leaves
   I.2.b step 1's not-live determination unresolved for a stale `launched` task — the
   consequence of that last effect is cited from `implement-phase.md` I.2.b, never
   restated (no Residual detail, no SC6 code).
3. Replace the purpose statement that the index exists solely to make a stop
   resolvable, so it covers resolving a stop and a recovery check back to a task.
4. Keep verbatim: "it carries no status semantics of its own, may be absent or stale"
   and "The sole exception to \"no status semantics of its own\" is the session
   identity" (with its following citation of the I.2.b Recovery / Residual block).

### R3 — Status semantics (unchanged, pinned)

The bullet containing "A `failed` task resolves ONLY by retry or by routing back to
planning" is not edited (D5). The new module pins it and pins that the bullet carries
no exception or carve-out clause.

## Acceptance Criteria (MANDATORY)

- [ ] AC-1 (FR7, NFR1): The writer-set paragraph contains `merge-unverified` stated as
  "a third additive value of the existing `failed` reason field", written by the same
  helper, invoked only from "`em-workflow/references/implement-phase.md`'s I.2.b step 1
  ancestor-check branch", adding no writer; the backtick-quoted .py/.sh names between
  "Its writer set is unambiguous" and "outside this one exception" equal exactly the
  five writers (negative proofs: a forged added name and a forged removed name are
  both detected).
- [ ] AC-2 (FR7): The paragraph no longer states that the helper is invoked only by
  the orphan-recovery attempt (absence assertion with a negative proof against the
  verbatim pre-change paragraph); the four existing phrases listed in R1 item 4 still
  match; the `failed_kind` definition text is byte-identical to its pre-change capture.
- [ ] AC-3 (FR6): The Status semantics bullet still contains "A `failed` task
  resolves ONLY by retry or by routing back to planning" and contains none of the
  carve-out markers "except", "unless", "not met", "exception"; a negative proof shows
  the check flags a forged copy of the bullet with an exception clause added.
- [ ] AC-4 (FR8): The agents.jsonl paragraph names all four readers of R2 item 1 and
  states that an absent or stale index also leaves I.2.b step 1's not-live
  determination unresolved for a stale `launched` task, citing `implement-phase.md`
  I.2.b; it does not contain the orchestrator-read rule's own wording ("most recently
  appended entry", "first-recorded candidate").
- [ ] AC-5 (FR8): "read by `queue_taskstop_net.py` at stop", "its absence only
  degrades the stop-tool recorder to a no-op" and "`agents.jsonl` exists solely to make
  a stop resolvable back to a task" are absent, each with a negative proof against the
  verbatim pre-change paragraph and a retained-anchor guard; the two kept phrases of R2
  item 4 still match.
- [ ] AC-6 (NFR4): `workflow-schema.md` contains none of the sixteen SC6 residual
  reason codes (one module-level constant listing the closed set); a negative proof
  shows the check flags a forged copy containing one code.
- [ ] AC-7 (FR10, NFR8): The new module imports only the standard library (AST
  check) and uses whitespace-normalized matching through module-level constants; the
  existing schema pins in `tests/test_implement_routeback_gate.py` and
  `tests/test_stale_launched_doc_contract.py` pass unmodified;
  `python3 -m unittest discover -s tests` passes.

## Test Notes

- Pre-change samples (writer-set paragraph, agents.jsonl paragraph, `failed_kind`
  definition) are copied verbatim from this task's base commit before editing
  (IMPLEMENTATION.md Convention 6).
- The five-name extraction reuses the same rule as the existing writer-set test
  (backtick-quoted tokens whose last path segment ends in .py or .sh).
- AC-3's bullet is sliced as the Status semantics list item that contains
  "`tasks.*.status` transitions" and ends at the next list item.
- Red first: AC-1, AC-4 and the AC-2/AC-5 absence matchers must be observed failing
  against the unedited document; AC-3 and AC-6 pin current text and are proven by
  their negative proofs.
- If an existing module's assertion contradicts this edit, stop and report it as a
  deviation (those modules are outside this task's files).

## Out of Scope

- `em-workflow/references/implement-phase.md` (including the Orchestrator-side read
  rule, which stays in its Supporting cast).
- Editing the Status semantics section or the `failed_kind` rules.
- Any script, hook or version file.
