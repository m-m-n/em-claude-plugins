# Task Plan: task0003 — I.2.b step 1 routes missing-artifact stale `launched` tasks through the proof chain; pin `bc57aa350bb027c7`

## Goal

`em-workflow/references/implement-phase.md` I.2.b step 1 states that a stale
`launched` task with one or both artifacts missing joins the same not-live
determination as the both-present candidates (Agent index lookup → Recovery → Orphan
recovery → Same-session extension), with artifact absence treated as evidence only and
Residual kept only for the unproven case; the Orphan recovery and Same-session
extension text matches IMPLEMENTATION.md SC-2; and a new doc-contract test pins the
candidate-set predicate that resolves `bc57aa350bb027c7` (finding `12839a507a7df994`
is resolved by the same edit).

## Requirements

FR1, FR2, FR3, FR10, NFR4, NFR5, NFR8

## Scope

### Files to Create

- `tests/test_stale_launched_missing_artifacts_doc_contract.py` — doc-contract tests for TS-1, TS-3 and the NFR4 reason-code placement check (TS-10, implement-phase.md half)

### Files to Modify

- `em-workflow/references/implement-phase.md` — the four I.2.b step 1 regions this task owns (IMPLEMENTATION.md D7), nothing else

## Design

Normative references: IMPLEMENTATION.md D1, D2, D6, D7, D8, SC-2, SC-5, Conventions
1, 2, 4, 5, 6, 10. The regions below are this task's only edit sites; every other line
of the file stays byte-identical.

### R1 — replace the "second, independent condition" block

Location: I.2.b step 1, first bullet (the in-flight existence check). The block
starts at the sentence beginning "A second, independent condition triggers this same
recovery" and ends with the sentence closing "... the existing gate-rejected terminal,
with the task named in the report." that sits immediately before "This recovery runs
during this wake-phase reconcile step". The replacement states, in prose:

1. The condition, all three conjuncts required: the task's journal last event is
   `launched`; at least one of the task worktree and the task branch does not exist —
   naming the both-absent state and each partial state explicitly; the task's
   `Task()` call is not among this reconcile step's own currently-outstanding calls.
2. Such a task joins the same not-live determination as the both-present candidate
   set above, in this order: the Agent index lookup (keyed by task id, needs no
   worktree, performed through the Agent index writer's orchestrator-read rule — cited,
   not restated, IMPLEMENTATION.md D6); Recovery (harness stop tool, then the
   stop-tool recorder's terminal `failed`); Orphan recovery (the legacy chain); the
   Same-session extension.
3. Artifact absence is observed evidence carried into that chain and never, by itself,
   proof that the agent terminated.
4. Unchanged, and named as unchanged: the outstanding-call exclusion, the termination
   conjunct, the stop-result conjunct, the agent-identity binding, and the
   append-time launch-identity recheck.
5. On proof: a terminal `failed` enters the journal under an existing reason
   (`orphaned`, `stale-launched`, or the stop-tool recorder's own), step 1 re-replays
   the journal within this same reconcile step, and steps 3 and 5 and I.2.c see
   `failed`, with retry and route back to planning both reachable.
6. Residual (journal unchanged, task in-flight, gate-rejected terminal, task named in
   the report) applies only when no proof is obtained — by citing the Residual
   statement above in this bullet, not by writing a second copy.
7. The phase's own role never includes writing the journal except through the
   Supporting cast Journal bullet's exception — cited, not restated.
8. No SC6 reason code is named anywhere in R1 (IMPLEMENTATION.md Convention 2).

### R2 — Orphan recovery candidate description

The parenthetical after "for exactly the not-live candidate set already established
above" currently enumerates "journal last event `launched`, task worktree and task
branch both present, Agent index lookup resolving to no live agent (...)". It is
rewritten to cover both the both-present candidates and the missing-artifact
candidates joined by R1, keeping the Agent-index clause and its three not-live cases.
The lead-in phrase itself stays verbatim.

### R3 — Same-session extension artifact step

The first evidence step currently reads "the task worktree and the task branch are
both observed present (else `task-artifacts-missing`)". It is rewritten to: each of
the two observations is supplied as exactly `yes` or `no` (else
`task-artifacts-missing`); both values are carried as evidence and neither ends the
chain (SC-2). The six codes of this extended chain keep their fixed relative order and
their first occurrences in I.2.b stay in that order.

### R4 — invocation contract, `--task-worktree`

The evidence-input list currently describes `--task-worktree <observed task worktree
path>`. It is rewritten so the value is the task's expected worktree path
(`$WT_ROOT/{T}`), supplied whether or not the path exists, which the identity-binding
step compares as a string with the Agent index entry's recorded worktree (SC-2).

### FR3 — pin without rewriting (D8)

No prose is changed for `bc57aa350bb027c7`. The candidate-set sentence ("Consequently,
the candidate set is exactly: ... whose `Task()` call is not among this reconcile
step's own currently-outstanding calls.") stays byte-identical. The new test pins that
predicate inside I.2.b together with the Same-session extension's existing
`stale-launched` invocation statement. Its negative proof uses a verbatim excerpt of
the loop-2 wording of `routeback-admissibility-exits` that restricted candidates to
`launched` events recorded before the current session started, located in the git
history of `em-workflow/references/implement-phase.md`: commit `0d9d0dc4`
(`fix(routeback-admissibility-exits): review round 3 loop 2`), lines 388–396 of that
file, including the sentence beginning "Only a task whose `launched` event was
recorded before the current session began". Take the excerpt verbatim from that
commit; never reconstruct it.

### Retained literals (pinned by existing modules; must still match after whitespace normalization)

- "the check FAILS when a task's journal last event is `launched` while the task
  worktree and the task branch both exist"
- "the allowed-but-never-started state, since I.2.a creates both artifacts before the
  launch call that records `launched`"
- "when the Agent index lookup is unresolvable or ambiguous, the journal is unchanged,
  the task stays in-flight, the route-back gate blocks, and the phase takes the
  existing gate-rejected terminal"
- "This recovery runs during this wake-phase reconcile step, hence before I.2.c's
  user-facing menu is offered"
- "reaches the normal failed handling in I.2.c, where retry and route-back are both
  available"
- "for exactly the not-live candidate set already established above"
- "the Agent index writer's orchestrator-read rule" (and I.2.b must not contain the
  orchestrator-read rule's own wording, which lives only in the Supporting cast)
- the Same-session extension's two conjunct sentences, its insufficiency sentence,
  "The recorded session identity is never used as the stop target on this branch",
  and "invoke `em-workflow/scripts/journal-append-failed.py` exactly once, with the
  task id and reason `stale-launched`"

## Acceptance Criteria (MANDATORY)

- [ ] AC-1 (FR1): I.2.b step 1 states the three-conjunct condition of R1 (journal
  last event `launched`; at least one artifact absent, with the both-absent and each
  partial state named; `Task()` call not among this reconcile step's own
  currently-outstanding calls) and the joining order Agent index lookup → Recovery →
  Orphan recovery → Same-session extension, with the lookup stated as keyed by task
  id, needing no worktree, and citing the Agent index writer's orchestrator-read rule.
- [ ] AC-2 (FR1, NFR5): The same text states that artifact absence is evidence and
  never by itself a termination proof; names the five unchanged elements as
  unchanged; states that on proof a terminal `failed` under an existing reason enters
  the journal, step 1 re-replays within the same reconcile step, and steps 3 and 5 and
  I.2.c see `failed` with retry and route back to planning reachable; and states that
  Residual applies only when no proof is obtained.
- [ ] AC-3 (FR1): "A second, independent condition triggers this same recovery
  without an Agent index lookup" and "no stop-tool call occurs and this condition
  falls to the same Residual treatment" are absent from `implement-phase.md`; each
  absence matcher has a negative proof against the verbatim pre-change R1 block with a
  retained-anchor guard; every retained literal listed in Design still matches.
- [ ] AC-4 (FR2): "journal last event `launched`, task worktree and task branch both
  present", "the task worktree and the task branch are both observed present (else
  `task-artifacts-missing`)" and "<observed task worktree path>" are absent from I.2.b
  (each with a negative proof); the R2, R3 and R4 statements are present; the six
  extended-chain codes still occur in their fixed first-occurrence order within I.2.b.
- [ ] AC-5 (FR3): A test pins, within I.2.b, the predicate "whose `Task()` call is not
  among this reconcile step's own currently-outstanding calls" and the Same-session
  extension's `stale-launched` invocation statement; a paired negative proof shows the
  loop-2 restriction literal is present in its verbatim sample and absent from the
  current I.2.b; the module docstring records `bc57aa350bb027c7` as resolved at HEAD
  with these two passages as the basis.
- [ ] AC-6 (NFR4): For each of the sixteen SC6 reason codes (one module-level constant
  listing the closed set), the count of its backtick-quoted token in the whole of
  `implement-phase.md` equals its count within I.2.b; a negative proof shows the check
  flags a forged copy with one code placed outside I.2.b.
- [ ] AC-7 (FR10, NFR8): The new module imports only the standard library (AST check),
  matches on whitespace-normalized text through module-level constants each read by a
  positive test and its negative proof; this task modifies no existing test module;
  `python3 -m unittest discover -s tests` passes.

## Test Notes

- Pre-change samples (R1 block, R2 parenthetical, R3 step, R4 flag description) are
  copied verbatim from `implement-phase.md` at this task's base commit before editing,
  each with a retained anchor present in both the sample and the live document
  (IMPLEMENTATION.md Convention 6).
- Section slicing reuses the existing markers (Convention 4): I.2.b is the text from
  "### I.2.b: Wake phase" to "### I.2.c: Failed handling".
- AC-6's count check treats a code absent from the whole document (count 0 in both)
  as passing.
- Red first: AC-1, AC-2 and AC-4's positive matchers and AC-3/AC-4's absence matchers
  must be observed failing against the unedited document before the edit; AC-5 and
  AC-6 are pins of existing text and are proven by their negative proofs instead.
- If an existing module's assertion turns out to contradict this task's edit, stop
  and report it as a deviation (that module is outside this task's files).

## Out of Scope

- I.2.a, I.2.b step 1's ancestor-check bullet, the Orphan recovery "This is the ONLY
  case ..." sentence, I.2.c, the Supporting cast (all owned by a sibling task, D7).
- `em-workflow/references/workflow-schema.md` and every script.
- Rewriting the candidate-set sentence or any other prose for `bc57aa350bb027c7`.
- Any change to the order, names or meaning of the SC6 codes beyond R3's statement.
