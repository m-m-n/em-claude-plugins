# Task Plan: task0001 — Precedence rule binds the abort terminal-commit stop to one code

## Goal

batch-terminal-line.md's 'Precedence rule:' paragraph resolves the one
phase-specific-vs-phase-specific collision, so the stop where the batch
second-failure abort's terminal status commit exhausts exit-4 recovery binds to
exactly one reason code. The em-workflow version reads 0.2.2 in both manifests.

## Requirements

FR1, FR2, FR3, FR8, FR10, NFR1, NFR2, NFR3, NFR4

## Scope

### Files to Create
- `tests/test_abort_terminal_commit_precedence.py` — new doc-contract tests for the Precedence paragraph (TS-1, TS-2)

### Files to Modify
- `em-workflow/references/batch-terminal-line.md` — extend the 'Precedence rule:' paragraph only
- `em-workflow/.claude-plugin/plugin.json` — set the version to 0.2.2
- `.claude-plugin/marketplace.json` — set the em-workflow entry's version to 0.2.2

## Design

### Region

Edit only the 'Precedence rule:' paragraph in the Stop point coverage section.
Existing tests slice this paragraph in two ways:

- from its label to the next blank line;
- from its label to the "No result on a wait turn" heading.

It must therefore stay a single paragraph with no blank line inserted. Append
the new sentences after the existing sentences, before the paragraph's closing
blank line. Leave the coverage table, the reason code list and every other
section untouched (NFR1).

### Statements to append (in this order)

1. **Phase-specific collision rule (FR1).** When a stop matches two phase-specific rows, the row whose commit failure directly caused the stop wins. The one such collision:
   - The batch second-failure abort's terminal status commit (Step I.2.c abort-phase terminal status commit) returns exit 4 on its first attempt and exit 4 again on its single exit-4 retry.
   - That stop matches both `implement-second-failure` and `docs-commit-conflict`.
   - It binds to `docs-commit-conflict` → `docs_commit_conflict_aborted`, not to `implement_task_failed`.
2. **Terminal stop (FR2).** This stop ends the run. The run never reaches a Step B entry evaluation that reads the uncommitted `implement: failed`, so neither `implement-second-failure` nor `stop-condition-3` applies to that run.
3. **Retry-success carve-out (FR2).** A first exit 4 followed by a successful retry commits the terminal status write. That stop keeps `implement-second-failure` → `implement_task_failed`.
4. **Written-but-uncommitted distinction (FR3).** Keep the existing sentence that `docs-commit-conflict` aborts without writing any status because the failed status write is itself its stop cause. Then add:
   - In the case of statement 1, the terminal status write (`implement: failed` / `failed_kind: decision`) exists in the integration worktree but stays uncommitted and never reaches the branch. This is distinct from "no status written".
   - How that leftover is disposed of and handled by the next run is defined by the abort terminal-commit exception in implement-phase.md's Branch & Worktree Model. Cite it by that label and section as a pointer only (IMPLEMENTATION.md D1, Shared Components).
5. **Scope (FR8).** Stops at the other implement exit-4 call sites keep their binding to `docs_commit_conflict_aborted` alone:
   - Step I.1 baseline
   - Step I.2.a launch
   - Step I.2.b wake
   - Step I.3 completion

### Constraints

- NFR2 anchors stay verbatim inside the paragraph:
  - "phase-specific stop point takes precedence over the generic"
  - "through that phase's own abort route"
  - "no route of the current run produced"
  - "write a step's status"
  - "aborts without writing any status"
  - "the failed status write is itself its stop cause"
  - the three stop-point keys
  - the order of the route-based `stop-condition-3` restriction before the `failed_kind` restriction
- Existing tests find some anchors by first occurrence. Appended text must not add an earlier occurrence of any existing anchor. Name the write with the Shared Components notation, never with the phrase "`failed_kind` reads `decision`".
- The paragraph never names `no-work-required`. Do not reintroduce the retired phrases "no phase-specific row covers" and "all three leave a step's status `failed`".
- Do not state re-derivation on resume or the `detail` / `resume_conditions` content here; those belong to implement-phase.md (IMPLEMENTATION.md D1).
- Version: set exactly 0.2.2 in both manifests (IMPLEMENTATION.md D3). Change no other manifest field.

## Acceptance Criteria (MANDATORY)

- [ ] AC-1 (FR1, TS-1): The 'Precedence rule:' paragraph (sliced from its label to the next blank line) meets all of:
  - it names both `implement-second-failure` and `docs-commit-conflict` as matching the stop where the Step I.2.c abort-phase terminal status commit returns exit 4 on its first attempt and on its single retry;
  - it states that the row whose commit failure directly caused the stop wins;
  - it binds that stop to `docs_commit_conflict_aborted`.
- [ ] AC-2 (FR2, TS-1): The same paragraph meets all of:
  - it states that this stop ends the run;
  - it states that no Step B evaluation of the uncommitted `implement: failed` happens in that run, so neither `implement-second-failure` nor `stop-condition-3` applies;
  - it states that a first exit 4 followed by a successful retry keeps `implement_task_failed`.
- [ ] AC-3 (FR3, TS-1): The same paragraph meets all of:
  - the existing "aborts without writing any status ... the failed status write is itself its stop cause" sentence is still present;
  - it states that in the AC-1 case the terminal status write exists in the integration worktree, uncommitted, and never reaches the branch;
  - it points to implement-phase.md's Branch & Worktree Model for its handling.
- [ ] AC-4 (FR8, NFR1, TS-1, TS-6): The same paragraph meets both of:
  - it confines the new resolution to that one collision;
  - it states that the Step I.1, I.2.a, I.2.b and I.3 exit-4 stops bind to `docs_commit_conflict_aborted` alone.

  The existing 13-code and 14-row pins in `test_batch_stop_contract.py` / `test_failed_kind_batch_docs.py` pass unchanged.
- [ ] AC-5 (NFR2, TS-1): The paragraph meets all of:
  - it contains no blank line;
  - every NFR2 anchor listed under Constraints is present;
  - it does not contain `no-work-required`.

  Every existing Precedence-paragraph test passes unchanged.
- [ ] AC-6 (FR1, TS-2): Every new matcher introduced for AC-1 to AC-4 fails against a verbatim sample of the pre-change paragraph captured from the base revision.
- [ ] AC-7 (FR10, NFR3, NFR4, TS-7, TS-8): Both manifests read `0.2.2` for em-workflow, and the existing version-lockstep test passes. `python3 -m unittest discover -s tests` shows no failures beyond the base's pre-existing ones. No script file is changed.

## Test Notes

- All new tests go in the new module. Use the same slicing semantics as `_extract_precedence_rule_paragraph` in the existing test_batch_stop_contract.py, from the label to the next blank line. Importing that helper or mirroring it is the implementer's choice.
- Normalize whitespace before phrase matching, following the existing modules. Assert on distinctive phrases, not whole sentences, so reasonable wording stays possible. The assertions must still make each of AC-1 to AC-4 impossible to satisfy vacuously; for example, the winner assertion should tie `docs_commit_conflict_aborted` to the abort terminal-commit statement.
- TS-2: pin the pre-change paragraph as a verbatim constant, taken from the base revision's file, and assert each new matcher rejects it. Include a non-vacuity check that the sample really is the Precedence paragraph, for example that it carries an existing anchor.
- Existing tests (TS-6, TS-7) are run, not modified.
- The main branch has 9 pre-existing failures. Compare the set of failing test IDs with the base run, not just the count.

## Out of Scope

- implement-phase.md, develop/SKILL.md and commit-docs.sh.
- Re-derivation on resume and `detail` / `resume_conditions` content for this stop (IMPLEMENTATION.md D1).
- Adding or changing any reason code or Stop point coverage row, and any other section of batch-terminal-line.md.
- Modifying existing test modules.
