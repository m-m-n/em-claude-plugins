# em-claude-plugins

Claude Code のプラグインを配布する個人マーケットプレイス。

開発ワークフローそのものをプラグインとして束ねている。仕様策定から並列実装、
多観点レビューまでを 1 本の流れとして回す `em-workflow` と、そのレビュー段だけを
単体で使えるようにした `em-review`。どちらも「人が張り付いていなくても、仕様に
沿った実装がレビューを通った状態まで進む」ことを目指している。

届ける相手は、このマーケットプレイスを追加した Claude Code 利用者。まず自分自身が
日々の開発でその最初の 1 人になる。
